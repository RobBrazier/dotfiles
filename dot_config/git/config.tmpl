[user]
	name = {{ .git.name }}
	email = {{ .git.email }}
	{{ if not (eq .git.signingkey "") }}signingkey = {{ .git.signingkey }}
{{ end }}
[pull]
	rebase = true

[push]
	default = current

[core]
	autocrlf = input
